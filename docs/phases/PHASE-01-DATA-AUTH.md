# Phase 01 — Data, Authentication and Authorization

Implement Supabase authentication, workspace tenancy, core canonical tables, migrations, seed data and row-level security. Prove that one workspace cannot read or change another workspace’s data. Keep service-role operations server-only.
